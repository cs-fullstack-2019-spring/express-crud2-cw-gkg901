extends layout

block content
    h1 Update a post

    form(method='GET' action='/updatePost')
        p
            label(for='id') Post ID to update:
            input(type='text', id='id', name='id')

        p
            label(for='title') Title:
            input(type='text', id='title', name='title')

        p
            label(for='body') Body:
            input(type='textarea', id='body', name='body')

        br
        button Submit

